# HU-004: Trazabilidad Completa del Pipeline vía Kafka

## Identificación

| Campo | Detalle |
|---|---|
| **ID** | HU-004 |
| **Épica** | Streaming y Observabilidad |
| **Prioridad** | Media-Alta |
| **Estimación** | 5 puntos |
| **Sprint** | 2 |

---

## Historia de Usuario

**Como** ingeniero de plataforma responsable del sistema,  
**quiero** que cada paso del flujo de una consulta del usuario quede registrado como un evento en Apache Kafka,  
**para** tener trazabilidad completa de extremo a extremo de cada interacción, poder auditar el comportamiento del sistema, calcular latencias por etapa y detectar cuellos de botella en el pipeline.

---

## Descripción

Cada servicio del sistema publica un evento en su topic de Kafka correspondiente cuando procesa una consulta. Los tres topics actúan como un log de auditoría distribuido: `user-requests` registra la entrada del usuario, `agent-actions` registra el razonamiento y acciones del agente IA, y `model-responses` registra la predicción generada. Un servicio consumidor independiente lee estos topics y calcula métricas de latencia end-to-end correlacionando eventos por `session_id`.

---

## Criterios de Aceptación

### Escenario 1: Evento publicado en user-requests al recibir consulta

**Given** que el servicio `web-ui` está corriendo y conectado a Kafka  
**When** el usuario envía una pregunta a través del chat  
**Then** se publica un mensaje en el topic `user-requests` con el siguiente esquema:
```json
{
  "timestamp": "<ISO8601>",
  "session_id": "<uuid>",
  "pregunta": "<texto de la pregunta>",
  "user_agent": "<browser info>"
}
```
**And** el mensaje se publica antes de llamar al servicio del agente  

### Escenario 2: Evento publicado en agent-actions al procesar la consulta

**Given** que el servicio `ai-agent` recibe y procesa una consulta  
**When** el agente completa su ciclo de razonamiento y obtiene la predicción  
**Then** se publica un mensaje en el topic `agent-actions` con el siguiente esquema:
```json
{
  "timestamp": "<ISO8601>",
  "session_id": "<uuid>",
  "pregunta_original": "<texto>",
  "provincia_extraida": "<PROVINCIA>",
  "mes_extraido": "<1-12>",
  "endpoint_construido": "POST /predict",
  "payload_enviado": {},
  "latencia_agente_ms": "<float>"
}
```

### Escenario 3: Evento publicado en model-responses al generar predicción

**Given** que el servicio `ml-inference` recibe y procesa una solicitud de predicción  
**When** el modelo genera la predicción exitosamente  
**Then** se publica un mensaje en el topic `model-responses` con el siguiente esquema:
```json
{
  "timestamp": "<ISO8601>",
  "session_id": "<uuid>",
  "provincia": "<PROVINCIA>",
  "mes": "<1-12>",
  "prediccion": "<float>",
  "modelo_version": "<v1|v2>",
  "latencia_ms": "<float>"
}
```

### Escenario 4: Fallo de Kafka no interrumpe el flujo principal

**Given** que el broker de Kafka no está disponible  
**When** cualquier servicio intenta publicar un evento  
**Then** el error se registra en el log del servicio con nivel WARNING  
**And** el flujo principal continúa y el usuario recibe su respuesta normalmente  
**And** no se lanza una excepción no manejada ni se retorna un error HTTP 5xx al usuario  

### Escenario 5: El consumidor correlaciona eventos por session_id

**Given** que los tres topics contienen eventos con el mismo `session_id`  
**When** el `kafka-consumer` procesa los mensajes  
**Then** calcula la latencia end-to-end como la diferencia entre el timestamp de `model-responses` y el timestamp de `user-requests` para ese `session_id`  
**And** expone esa latencia como métrica Prometheus `pipeline_end_to_end_latency_seconds`  

---

## Definición de Done

- [ ] Los tres topics (`user-requests`, `agent-actions`, `model-responses`) son creados automáticamente al iniciar Kafka
- [ ] Cada servicio tiene su propio `kafka_producer.py` con manejo de errores fail-safe
- [ ] El `session_id` se genera en `web-ui` y se propaga a todos los servicios del pipeline
- [ ] El esquema JSON de cada topic está documentado en el README
- [ ] El servicio `kafka-consumer` consume los tres topics correctamente
- [ ] La latencia end-to-end se calcula y expone como métrica Prometheus
- [ ] Los mensajes de error de Kafka aparecen como WARNING en los logs, no como ERROR crítico
- [ ] En el `docker-compose.yml` los topics se crean con un servicio `kafka-init`
- [ ] Tests de integración validan que los eventos se publican con el esquema correcto
